namespace DFramework.MyStorage.SDK
{
    public enum ErrorCode
    {
        NoError,
        FileIsNull,
        FileNotExists,
        NoValidStorageNode,
        NoFileUploaded,
        FileNotComplete,
        Unknown = -1
    }
}